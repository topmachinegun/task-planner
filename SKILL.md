---
name: crm_task_planner
description: 对 ClawCRM 跟进中项目做规则化缺口巡检，自动生成三类待办任务：(1)客户基本信息不完整 (2)项目AI评估后提取可执行动作 (3)项目超过14天未跟进。不做发散式销售规划，任务类型收窄至三种预设类型。当用户说"巡检项目"、"检查项目缺口"、"扫描待跟进项目"、"检查评估建议"时触发。
---

> **本技能为 L3 业务技能**，保留在 hap-skill-claw-lite monorepo 中。
> 分发版在独立 GitHub repo：[`task-planner`](https://github.com/topmachinegun/task-planner)。

# 项目缺口巡检

对 ClawCRM 跟进中项目做规则化扫描，针对三类缺口自动创建待办任务。

**任务类型收窄**：仅允许三种预设类型（见 §4），评估建议中不匹配的部分直接丢弃。

## 1. 触发条件

触发短语：
- "巡检项目 / 检查项目缺口 / 扫描待跟进项目"
- "扫描客户信息缺失"
- "检查项目搁置"
- "检查评估建议 / 根据AI评估生成任务"

## 2. 前置条件

| 项目 | 默认值 | 缺失时行为 |
|---|---|---|
| Token | L2 `token_reader.get_mcp_url("claw-crm")` | 报错退出 |
| ClawCRM appId | `49392ae2-6aa0-4d69-b5e7-57d4fe3fc98e` | 硬编码 |
| 项目管理工作表 | `69ca1fb1d128aadb0c749d49` | 硬编码 |
| 任务管理工作表 | `69ca1fc12304b0580845c0c2` | 硬编码 |
| 客户档案工作表 | `69ca1f9f2304b0580845be9d` | 硬编码 |

## 3. 三种触发场景

### 3.1 场景一：客户信息模糊（语义判断）

> ⚠️ **关键区别**：不检查字段是否为空、不检查是否关联客户档案。只判断信息是否模糊。

**条件**：项目状态 = "跟进中"，且公司名称或联系人姓名存在以下语义模糊：
- 公司名称为占位符/测试值（如 "测试"、"ABC"、"待定"、"某某公司"、纯数字等），而非具体可识别的企业名
- 联系人姓名为非正常中英文姓名（如 "测试"、"123"、"AAA" 等），而非具体的人名

**判断主体**：由智能体对 `customer_snapshots` 输出做语义判断，脚本不做二元判断。

**动作**：对语义判断为模糊的项目，由智能体调用 `create_task`（可通过脚本的 `--project` / `--row-id` 模式逐个创建），任务名 `"{项目名} - 完善联系人信息"`。**负责人字段必须从 snapshot 的 `owner_ids` 中获取并写入。**

### 3.2 场景二：项目搁置检测

**条件**：项目状态 = "跟进中"，且最后跟进时间距今超过 14 天。

**动作**：创建一条任务，任务名格式 `"{项目名} - 项目长期未更新（{N}天），需跟进"`，任务类型「客户背景调查」。任务说明注明搁置原因及时长。

### 3.3 场景三：AI评估提取动作（关键词映射）

**条件**：项目状态 = "跟进中"，且 AI评估 字段不为空。

**判断主体**：脚本收集 AI评估 内容输出至 `evaluated_projects`，智能体阅读评估报告，从中提取「下一步动作」建议句，并按下表关键词映射到任务类型。**不匹配任何关键词的动作直接丢弃。**

**关键词 → 任务类型映射**（仅在三种预设类型内收敛）：

| 评估建议中的关键词 | 任务类型 | 任务名称格式 |
|---|---|---|
| 方案 / 提案 / 演示材料 / PPT | 项目方案输出 | `"{项目名} - {评估建议摘要}"` |
| 背景 / 决策链 / 角色 / 信息 | 客户背景调查 | `"{项目名} - {评估建议摘要}"` |
| 电话 / 致电 / 回电 | 打电话 | `"{项目名} - {评估建议摘要}"` |

> 超出以上关键词范围的动作（如拜访、案例、报价、POC 等）→ **不创建任务**。

**去重**：创建前检查是否已存在同名任务，避免重复创建。

## 4. 任务类型（收窄为三种）

| 任务类型 | key | 适用场景 |
|---|---|---|
| 客户背景调查 | `382f2695-5304-4107-88a3-8151ed2a90e3` | 场景一、场景二、场景三（背景/决策链/角色/信息） |
| 项目方案输出 | `ab43d3e5-635e-46ef-aa1e-ad39c0bf062a` | 场景三（方案/提案/演示材料/PPT） |
| 打电话 | `93e0f6e0-adc2-40ea-9ec0-dbc37532ac5c` | 场景三（电话/致电/回电） |

场景三的映射规则见 §3.3，不匹配关键词的动作不创建任务。

## 5. 任务字段写入规则

| 任务字段 | 写入值 |
|---|---|
| 任务名称 | 场景一 `"{项目名} - 完善联系人信息"` / 场景二 `"{项目名} - 项目长期未更新（{N}天），需跟进"` / 场景三 `"{项目名} - {评估建议摘要}"` |
| 任务类型 | 场景一/二「客户背景调查」`382f2695-5304-4107-88a3-8151ed2a90e3`；场景三根据关键词映射（§3.3）选 `382f2695...` / `ab43d3e5...` / `93e0f6e0...` |
| 任务来源 | 固定 `"AI"` |
| 任务状态 | 固定 `"待接收"`（key: `c60b3807-e98f-45e2-a9af-b147b3fc4ea7`） |
| 负责人 | 项目「负责人」字段值 |
| 项目 | 关联当前项目 rowId |
| 任务结束时间 | 留空 |
| 任务说明 | 场景一"因该项目客户信息存在模糊：..." / 场景二"因项目N天未更新：...，需排查原因并推动进展。" / 场景三"基于AI评估建议：{动作原文}" |

## 6. 端到端流程

```
- [ ] S1 通过 token_reader.get_mcp_url("claw-crm") 读取 MCP URL
- [ ] S2 initialize + tools/list，缓存 tools schema
- [ ] S3 获取跟进中项目列表（project_status = "跟进中"）
- [ ] S4 对每个项目：
- [ ] S4a 场景一：收集客户信息快照 → 输出 `customer_snapshots` 数组，智能体对每项做语义判断：公司名是否具体可识别？联系人姓名是否正常？
- [ ] S4b 场景一（续）：智能体对语义模糊的项目调用 `create_task`（任务名 `"{项目名} - 完善联系人信息"`）
- [ ] S4c 场景二检查：最后跟进 >14 天 → 则创建搁置任务（脚本自动）
- [ ] S4d 场景三：收集 AI评估 → 输出 `evaluated_projects`，智能体提取动作并映射任务类型、创建任务
- [ ] S5 输出 JSON 创建结果
```

## 7. 便捷方式：运行辅助脚本

```bash
# 试运行（只扫描+打印计划，不写入）
python3 skills/crm_task_planner/src/generate_tasks.py --dry-run

# 正式运行
python3 skills/crm_task_planner/src/generate_tasks.py

# 仅处理特定项目
python3 skills/crm_task_planner/src/generate_tasks.py --project "华北油田"
python3 skills/crm_task_planner/src/generate_tasks.py --row-id <ROW_ID>
```

## 8. 常见陷阱

| 现象 | 根因 | 解法 |
|---|---|---|
| 智能体自由发挥生成销售行动类任务 | description/触发词太宽，被理解为通用任务规划 | 本技能仅输出三种任务类型（§4），评估建议不匹配关键词的直接丢弃 |
| 任务创建后项目关联为空 | `add_record` 的关联字段格式不对 | 关联记录字段写 `[{"sid": "<rowId>"}]` |
| 任务类型写入失败 | 传了显示文本而非 key | 必须传 option key（UUID） |
| 联系人子表数据拿不到 | 子表需额外请求 | 先拿客户档案 row，再用 `get_record_relations` 或直接查子表 |
| AI评估为空但实际有内容 | `get_record_list` 不返回富文本字段 | 脚本已改用 `get_record_details` 获取 AI评估 |

## 9. Related

- L2 `skills/hap_app_access/` — HAP 通用访问方法论 + 共享模块
- L3 `skills/crm_project_review/` — 项目评审技能（AI评估生产者）
- 分发 repo：[`task-planner`](https://github.com/topmachinegun/task-planner)

---

**技能版本**：v1.2.0
**适用范围**：明道云 HAP（SaaS）

**v1.2.0**：新增场景三（AI评估驱动任务），任务类型收窄为三种预设类型（客户背景调查/项目方案输出/打电话），含关键词映射表。脚本新增 `evaluated_projects` 输出，改用 `get_record_details` 获取 AI评估 富文本字段。
**v1.1.0**：修复项目搁置检测场景二→场景三重编号。
**v1.0.0**：初始版本，支持两种触发场景（客户信息完善 + 项目搁置检测）。
